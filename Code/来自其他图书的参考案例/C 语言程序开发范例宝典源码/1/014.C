#include<stdio.h>
main()
{
    char a[5] =
    {
        '*', '*', '*', '*', '*'
    };									/*定义字符型数组，五个元素初值均为'*'*/
    int i, j, k; 								/*定义变量i，j，k为基本整型*/
    for (i = 0; i < 5; i++)						/*输出5行*/
    {
        for (j = 1; j <= i; j++)					/*输出空格的数量随着行数的变化而变化*/
            printf(" ");
        for (k = 0; k < 5; k++)
            printf("%c", a[k]);					/*将a数组中的元素输出*/
        printf("\n"); 							/*每输出一行后换行*/
    }
}
